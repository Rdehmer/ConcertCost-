# Concert Cost Tracker

Track concerts you attend, what you spend, and how much fun you had.

## Getting started

1. Copy `.env.local.example` to `.env.local` and add your Supabase Project URL and anon/publishable key.
2. Run `npm install`
3. Run `npm run dev`
4. Open [http://localhost:3000](http://localhost:3000)
